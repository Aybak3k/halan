//- includes manager
include ../includes/spc-manager.pug
+loadBasicIncludes()

//- TEMP << cz local vars are lost in 2 includes..
include ../includes/spc-data.pug

//- Mixins
mixin customHead()
    link(rel="stylesheet", href=indexStylesPath+"index.css")

//- DOCTYPE
doctype html
html(lang="en")

    //- <head>
    +head(brandName, customHead)

    //- <body>
    body 
        header
            +navBar(logo, indexLinks)

        //- Essential Scripts
        +loadScripts(indexScriptsPath)